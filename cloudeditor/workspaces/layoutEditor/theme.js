const layoutEditor = require("../../themes/layoutEditor/layoutEditor.css");
const sidebar = require("../../themes/layoutEditor/Sidebar.scss");
const theme = require("../../themes/layoutEditor/theme.scss");
const editor_icons = require("../../themes/layoutEditor/editor_icons.css");
const addImage = require("../../themes/layoutEditor/addImage.scss");
const addPdf = require("../../themes/layoutEditor/addPdf.scss");
const toolbar = require("../../themes/default/toolbar/plugin.scss");
const chromoselector = require("../../core/assets/chromoselector/chromoselector.css");

module.exports = {
  layoutEditor,
  sidebar,
  theme,
  editor_icons,
  addImage,
  addPdf,
  toolbar,
  chromoselector
};
